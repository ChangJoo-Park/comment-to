specVersion: "v1beta"
serviceId: "comments"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "comments-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
